The codebase currently has test or linting failures. Please fix them.

PROJECT ARCHITECTURE:
{architecture_type}

You are explicitly authorized and encouraged to:
1. Fix the underlying issues in the implementation code.
2. Fix or update the test code if the tests are incorrect or outdated.
3. Install missing Python requirements (e.g., `pip install ...`) or Node.js requirements (`npm install ...`) if dependencies are missing.
4. Adjust environment variables, configuration files, or the testing environment as needed.
5. Fix Makefile targets that use the wrong tool for a component (e.g., if a Makefile target runs `npm` against a Python directory, change it to use `pytest`/`ruff` instead).

You are NOT authorized to:
- Install system tools (helm, kubectl, kind, minikube, stern, skaffold) via Homebrew or any package manager.
- Create shim package.json files in Python directories to satisfy npm commands.

ERROR OUTPUT:
{test_output}

TASK:
1. Analyze the errors provided above.
2. Fix the issues — match test/build commands to the actual stack (pytest/ruff for Python, npm/vitest for Node.js).
3. Ensure that after your changes, the target tests pass.
4. Include <promise>DONE</promise> in your response once you believe the issues are fixed.
