[build-system]
requires = ["setuptools>=77.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vibe-tools"
version = "0.0.66"
description = "Local CLI utilities for working inside repo directories."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
dependencies = [
    "click",
    "google-genai",
    "pyyaml",
    "rich",
    "pydantic",
    "prompt_toolkit",
    "python-dotenv",
    "httpx",
    "gspread",
    "google-auth",
    "google-auth-oauthlib",
    "google-auth-httplib2",
]

[project.scripts]
vibe = "vibe_tools.cli:cli"
vibe-setup = "vibe_tools.setup:setup_cli"

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-cov",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
